{% extends 'base.html.twig' %}

{% block body %}
    <h1>Departamentotipo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ departamentoTipo.id }}</td>
            </tr>
            <tr>
                <th>Departamento</th>
                <td>{{ departamentoTipo.departamento }}</td>
            </tr>
            <tr>
                <th>Abrev</th>
                <td>{{ departamentoTipo.abrev }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('departamentotipo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('departamentotipo_edit', { 'id': departamentoTipo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
